{
  "name": "marees-port-tudy",
  "version": "2.0.0",
  "description": "Application client/serveur des marées de Port-Tudy (île de Groix) : API REST Node.js + dashboard Vue 3.",
  "private": true,
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm -w server run dev\" \"npm -w client run dev\"",
    "build": "npm -w server run build && npm -w client run build",
    "start": "npm -w server run start",
    "test": "npm -w server run test && npm -w client run test"
  },
  "keywords": [
    "marées",
    "port-tudy",
    "groix",
    "navihan"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "allowScripts": {
    "esbuild": true
  }
}
